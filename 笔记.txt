1,输入框不能使用tableview的复用机制，离屏后无法获取cell内容,使用成员变量方法规避tableview的复用机制

2，_contentLabel.numberOfLines = 0;//自动换行,ios10只需这句
   _contentLabel.preferredMaxLayoutWidth = SCREENWIDTH;//自动换行，ios9需要
   
3,有sectionhead的情况下，cell的高度不能为0

4,masonry,label不设置高度，并且内容为空的时候，高度为0

5,ios11 tableview默认header，footer高度置0
tableView.estimatedSectionHeaderHeight = 0;
tableView.estimatedSectionFooterHeight = 0;

6,符号化
atos -arch arm64 -o /Users/zc/Downloads/dddd/AmpMind.app.dSYM/Contents/Resources/DWARF/AmpMind -l 0x100d2c000 0x1012ee800

7,scrollview
contentsize不够又需要弹性：alwaysBounceVertical = YES;
禁止弹性:bounces = NO;

8,NSMutableArray必须用strong，而NSArray必须用copy

9,view model分离

10，导航统一，透明度，内边距

11，少用分类修改属性，可以增加，但不要修改

12，网络接口api用block,按钮事件用delegate,可以考虑用rac

13,初始化和布局分离

15,映射

14,git


git clone ssh://zengchao@gerrit.it.chehejia.com:29418/ampgo/ios/ampgo-ios
git checkout -b develop origin/develop
git add -A
git commit -m "test123"
gitdir=$(git rev-parse --git-dir); scp -p -P 29418 zengchao@gerrit.it.chehejia.com:hooks/commit-msg ${gitdir}/hooks
commit --amend
git push origin develop:refs/for/develop

打开fork
打开项目
执行:
git config remote.origin.push refs/heads/*:refs/for/*

16,注释工具

17,weex

18,动态参数

/***<#type#>*/
@property (retain,nonatomic) <#type#> *<#name#>;

19，代码片段
打init，出
- (instancetype)init
{
    self = [super init];
    if (self) {
        <#statements#>
    }
    return self;
}

20.注释快捷键
option+command+/

21.注释类模版
/**
 * <pre>
 *     @author : Blankj
 *     e-mail : xxx@xx
 *     time   : 2017/03/07
 *     desc   : xxxx描述
 *     version: 1.0
 * </pre>
 */
public class WelcomeActivity {
       ...
}
可以通过修改xcode模版操作：
cd到这个目录下
/Applications/Xcode.app/Contents/Developer/Platforms/iPhoneOS.platform/Developer/Library/Xcode/Templates/File Templates
新建Chj
拷贝Source/Cocoa Touch Class.xctemplate到Chj
修改
Chj/Cocoa Touch Class.xctemplate/NSObjectObjective-C/___FILEBASENAME___.h
Chj/Cocoa Touch Class.xctemplate/NSObjectObjective-C/___FILEBASENAME___.m

里面的类是锁定的，要修改需要解锁，那么下面就使用命令行来解锁：
用命令行 sudo chmod -R 777 文件名
修改模版
/**
* <pre>
* @author : ___FULLUSERNAME___ 
* @e-mail : xxx@chehejia.com
* @time : ___DATE___
* @desc : 
* @version: 1.0
* </pre>
*/

22,git的amend命令
在上次提交的基础上，重新提交一次修改，"修改id"不变.

24,子项目引用pod,需要在podfile里新增配置信息

25,git两次提交合并为一次提交:
git reset --soft HEAD^ //将节点指向第一个节点
git rebase origin/develop //合并
git add -A
git commit --am //重新提交
手动解决冲突（fork工具）
git rebase --continue
amend (删除changeid)
推送

26.